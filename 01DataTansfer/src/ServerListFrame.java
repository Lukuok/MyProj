import javax.swing.JFrame;

public class ServerListFrame extends JFrame
{
    ServerListFrame()
    {
        setSize(500, 500);
        setVisible(true);   // 화면에 프레임 창을 표시
        System.out.println("ServerListFrame 실행됨");
    }
}
